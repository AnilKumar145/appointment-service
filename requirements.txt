fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
psycopg2-binary==2.9.9
alembic==1.13.2
pydantic==2.9.2
python-multipart==0.0.9
pillow==10.4.0
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# Additional dependencies for running the pytest suite
# Install alongside the app requirements.txt

# Core testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# HTTP client for async/perf tests
httpx==0.27.2

# Optional: contract and property-based testing of OpenAPI
schemathesis==4.1.4

# Optional: load testing tool (not strictly required for current tests)
locust==2.31.3
